#include "main.h"

/**
 * main - Entry point
 * Description: adds two integers and returns the result
 * Return: Always 0
 */
 int add(int a, int b)
{
       int c;

       c = a + b;
       return (c);
}

	
